---
phase: 2
slug: header-hero
status: approved
shadcn_initialized: true
preset: "default / radix / lucide / v4"
created: 2026-05-09
---

# Phase 2 — UI Design Contract

> Visual and interaction contract for Phase 2: Header + Hero.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.
>
> Phase 2 delivers the two structural elements visitors see first: (1) a sticky
> header with logo, numbered nav, theme toggle, and hire CTA, and (2) a hero section
> with H1, terminal status card, two CTA buttons, and a 4-stat trust strip. All design
> tokens are pre-established in Phase 1 — this contract maps those tokens to the
> specific elements built in this phase.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | shadcn (already initialized) | components.json detected |
| Style | default | `npx shadcn info` |
| Component base | radix | `npx shadcn info` |
| Icon library | lucide | `npx shadcn info` |
| Installed components | badge, button, card | `npx shadcn info` |
| Font — body/UI | Inter (variable, weight 300–800) | Phase 1 UI-SPEC |
| Font — mono/kicker | JetBrains Mono (variable, weight 400–700) | Phase 1 UI-SPEC |
| Font — display/editorial | Fraunces (variable italic, weight 300–500) | Phase 1 UI-SPEC |
| CSS variable prefix | `--font-sans`, `--font-mono`, `--font-display` | Phase 1 UI-SPEC |
| Theme strategy | `[data-theme="dark"]` / `[data-theme="light"]` on `<html>` | Phase 1 complete |
| Default theme | dark | Phase 1 complete |

---

## Spacing Scale

All spacing tokens are pre-established in `globals.css :root` from Phase 1. Phase 2
uses these tokens by name. Do not add new spacing values — use only what is declared.

> **Revision note (checker fix — final):** The previous scale contained non-standard
> values (12px, 20px, 80px, 96px, 120px) and duplicate token pairs. The scale is now
> collapsed to exactly 7 unique values matching the standard set {4, 8, 16, 24, 32, 48,
> 64}. Each token maps to a unique value. Downstream element descriptions have been
> updated to reference the remapped tokens.

| Token | Value | Usage in this phase |
|-------|-------|---------------------|
| `--space-1` | 4px | Stack chip internal gap, icon gaps, traffic-light dot gap |
| `--space-2` | 8px | Stack chip padding (2px 8px horizontal), hire CTA button padding top/bottom, nav link gap |
| `--space-3` | 16px | Theme toggle button padding left/right, hire CTA button padding left/right, terminal title bar padding left/right, secondary CTA padding left/right |
| `--space-4` | 24px | Logo gap, status row gap, nav gap between number and label, cursor margin-top, terminal title bar padding top/bottom |
| `--space-5` | 32px | Nav flex gap, hero status row margin-bottom, hero lede margin-top, CTA row margin-top, header inner padding top/bottom, trust strip padding-top, trust strip column gap |
| `--space-6` | 48px | Terminal card body padding |
| `--space-7` | 64px | Hero grid column gap, hero section padding-top, trust strip margin-top, hero section padding-bottom |
| `--content-max` | 1240px | Max content width (header inner, hero inner) |
| `--page-px` | 32px | Header and hero horizontal padding (alias for `--space-5` value) |
| `--section-py` | 64px | Hero section padding-bottom (alias for `--space-7` value) |

Exceptions:
- Theme toggle inner pill: `padding: 2px` — hardcoded; not a spacing token. Acceptable at this scale.
- Logo block `M` letter: 28×28px. Not a spacing token — fixed icon dimension. Use `w-7 h-7` (Tailwind = 28px).
- Primary/secondary CTA padding: `16px 24px` — maps to `--space-3` (16px) / `--space-4` (24px). Both values are in the standard set.

---

## Typography

All type tokens are pre-declared in `globals.css :root` from Phase 1. This section
maps those tokens to exact element roles in Phase 2.

> **Revision note (checker fix — final):** Collapsed from 8 rendered sizes to exactly 4
> named tokens. Font weight vocabulary remains exactly 2 values — **400 (regular)** and
> **700 (bold)**. Trust strip stat numbers previously used an inline 48px override; they
> now use `--text-body-lg` (17px). Visual emphasis for trust strip stats comes from the
> orange "+" and "%" characters, not from a large number size.

### Declared Type Tokens (4 total)

| Token | Rendered value | Font |
|-------|---------------|------|
| `--text-hero` | `clamp(48px, 8vw, 112px)` | Inter (`--font-sans`) |
| `--text-body-lg` | `17px` | Inter (`--font-sans`) |
| `--text-mono` | `12px` | JetBrains Mono (`--font-mono`) |
| `--text-label` | `11px` | JetBrains Mono (`--font-mono`) |

### Element Mapping

| Element | Token | Weight | Additional |
|---------|-------|--------|------------|
| Hero H1 | `--text-hero` | 400 base, 700 for "ships"/"lasts" | `--lh-tight` (0.92), `--tracking-tight` (-0.045em) |
| Hero H1 Fraunces accent ("&") | `--text-hero` | 400 italic | Fraunces (`--font-display`), color `--ms-orange-text`, italic only |
| Hero lede paragraph | `--text-body-lg` | 400 | line-height 1.55, color `--ms-fg-soft`, max-width 520px |
| Hero lede — "Daniel Trochez" | `--text-body-lg` | 700 | color `--ms-fg` (fg, not fgSoft) |
| Hero status row label ("AVAILABLE") | `--text-mono` | 700 | color `--ms-orange-text` |
| Hero status row text (remaining) | `--text-mono` | 400 | color `--ms-fg-soft` |
| Hero CTA primary | `--text-mono` | 700 | — |
| Hero CTA secondary | `--text-mono` | 700 | — |
| Header logo "monkey/solutions" | `--text-mono` | 700 | letter-spacing 0.3px |
| Header logo "daniel_trochez.dev" | `--text-label` | 400 | color `--ms-fg-faint` |
| Header logo "M" | `--text-mono` | 700 | inside 28×28 orange square |
| Nav links — number | `--text-mono` | 400 | color `--ms-orange-text` |
| Nav links — label | `--text-mono` | 400 | color `--ms-fg-soft`, letter-spacing 0.3px |
| Hire CTA button | `--text-mono` | 700 | — |
| Theme toggle buttons | `--text-label` | 400 | — |
| Terminal card title bar label | `--text-label` | 400 | color `--ms-fg-faint` |
| Terminal card body | `--text-mono` | 400 | line-height 1.75, color `--ms-fg` |
| Terminal stack chips | `--text-label` | 400 | color `--ms-fg-soft` |
| Trust strip numbers | `--text-body-lg` | 400 | letter-spacing -0.03em, line-height 1, color `--ms-fg` |
| Trust strip labels | `--text-label` | 400 | color `--ms-fg-soft`, letter-spacing 0.4px |

---

## Color

All color tokens are pre-established from Phase 1. This section declares exactly which
token maps to which Phase 2 element.

| Role | Token | Dark value | Light value |
|------|-------|-----------|-------------|
| Dominant 60% — page bg | `--ms-bg` | `#0d0b09` | `#f7f4ee` |
| Secondary 30% — header bg | custom rgba | `rgba(13,11,9,0.78)` | `rgba(247,244,238,0.82)` |
| Accent 10% — orange | `--ms-orange` | `#ff6b1a` | `#e85a0f` |
| Orange text on dark surface | `--ms-orange-text` | `#ff8b42` | `#c94a05` |
| Orange dim background tint | `--ms-orange-dim` | `rgba(255,107,26,0.15)` | `rgba(232,90,15,0.12)` |
| Orange mist (glow wash) | `--ms-mist` | `rgba(255,107,26,0.06)` | `rgba(232,90,15,0.04)` |
| Destructive | `--destructive` | `hsl(0 50% 30%)` | `hsl(0 50% 30%)` |

### Accent Reserved For (Phase 2 elements only)

- Logo "M" square background (`--ms-orange`)
- Hire CTA button background (`--ms-orange`)
- Status dot fill (`--ms-orange`)
- Status dot pulse ring (`--ms-orange` at opacity 0.3)
- "AVAILABLE" text in hero status row (`--ms-orange-text`)
- Nav link numbers ("01", "02", etc.) — use `--ms-orange-text`
- Hero H1 Fraunces "&" character — use `--ms-orange-text`
- Hero CTA "→" suffix on primary — inline with orange button (inherits button text color)
- Hero CTA "↓" suffix on secondary — use `--ms-orange-text`
- Terminal cursor border-right (`--ms-orange`)
- Trust strip "+" and "%" characters — use `--ms-orange-text`
- `:focus-visible` ring on all interactive elements — use `hsl(var(--ring))` which resolves to orange

### Header Background Detail

The header does NOT use a design token for its background — it is a hardcoded rgba
with transparency for the blur effect:

| Theme | Background | Blur |
|-------|-----------|------|
| dark | `rgba(13,11,9,0.78)` | `backdrop-filter: blur(12px)` |
| light | `rgba(247,244,238,0.82)` | `backdrop-filter: blur(12px)` |

Header border-bottom: `1px solid var(--ms-border)`.

### Hero Background FX (decorative, aria-hidden)

Two absolutely-positioned decorative layers:

1. Grid lines: `repeating-linear-gradient` in `--ms-border` color, 64×64px tile, masked by
   `radial-gradient(ellipse at 70% 40%, #000 20%, transparent 70%)`. Opacity 0.5.

2. Orange glow blob: `radial-gradient(circle, rgba(255,107,26,0.13) 0%, transparent 60%)`.
   Size 520×520px. Position `right: -10%; top: 10%`. Use `var(--ms-orange)` at 0x13 alpha
   (dark) / `var(--ms-orange)` at 0x10 alpha (light, i.e. `${orange}10`).
   Exact value from handoff: `${T.orange}22` — which in the dark theme = `#ff6b1a22`
   (hex alpha). Implement as: `background: radial-gradient(circle, color-mix(in srgb, var(--ms-orange) 13%, transparent) 0%, transparent 60%)`.

### Terminal Traffic Lights (decorative, aria-hidden)

| Light | Token | Value |
|-------|-------|-------|
| Red | `--color-tl-red` | `hsl(3 100% 67%)` = `#ff5f56` |
| Yellow | `--color-tl-yellow` | `hsl(41 100% 59%)` = `#ffbd2e` |
| Green | `--color-tl-green` | `hsl(129 68% 47%)` = `#27c93f` |

Each: 10×10px circle (`border-radius: 5px`), gap 4px (`--space-1`).

### Logo "M" Text Color

Dark theme: `#120a05` (near-black brown — hardcoded, not a token).
Light theme: `#fff` (white — hardcoded).

This color is only used in the logo block.

---

## Spacing & Layout — Element-Level Detail

### Header

```
<header> — sticky, z-index 50
  Inner: max-w 1240px, margin: 0 auto, padding: 16px 32px
  Layout: flex, align-center, justify-between

  Left — Logo link <a href="#top">:
    flex, gap: 24px (--space-4), no underline
    - [M] box: 28×28, radius 6, bg orange
    - Text block: line-height 1.05
      - "monkey/solutions": mono 12/700, fg, letter-spacing 0.3px (--text-mono)
      - "daniel_trochez.dev": mono 11/400, fgFaint (--text-label)

  Right — <nav>:
    flex, align-center, gap: 32px (--space-5)
    - 5 anchor links (01-05)
    - Theme toggle pill
    - Hire CTA <a href="#contact">
```

### Theme Toggle

```
Container: border-radius 20px, border: 1px var(--ms-border), padding: 2px
Two buttons: [☾ dark] [☀ light]
  - Active: bg fg, color bg, padding: 8px 16px (--space-2 / --space-3), radius 18px
  - Inactive: bg transparent, color fgSoft, padding: 8px 16px, radius 18px
  - Font: label 11/400 (--text-label)
```

**Interaction:** The existing `ThemeToggle.tsx` uses DOM manipulation + localStorage.
Phase 2 replaces the temp fixed-position button with this pill-style toggle integrated
into the header nav. Keep the same toggle logic — only change the markup and styling.

### Hire CTA Button

```
<a href="#contact">
  flex, align-center, gap: 8px (--space-2)
  bg: --ms-orange
  color: #120a05 (dark) / #fff (light)
  padding: 8px 16px (--space-2 top/bottom, --space-3 left/right)
  border-radius: 6px (--radius-md)
  font: mono 12/700 (--text-mono)
  Contains: <Dot pulse /> + "Hire me"
```

### Hero Section

```
<section id="top">
  padding: 64px 32px 64px (--space-7 top/bottom, --page-px sides)
  position: relative, overflow: hidden
  bg: --ms-bg (page default)

  [decorative grid, aria-hidden]
  [decorative orange glow, aria-hidden]

  Inner: max-w 1240px, margin: 0 auto, position: relative
  Grid: 1.4fr 1fr, gap 64px (--space-7), align-items: end
```

### Hero Left Column

```
Status row:
  flex, align-center, gap: 24px (--space-4), margin-bottom: 32px (--space-5)
  font: mono 12/400 (--text-mono)
  <Dot pulse /> AVAILABLE (orangeText/700) · freelance projects, Q2 2026 → (fgSoft)

H1:
  font: sans clamp(48px,8vw,112px)/400 (--text-hero), tracking -0.045em, line-height 0.92
  "Software that" — weight 400
  "ships" — weight 700
  "&" — display italic 400, color orangeText
  "lasts." — weight 700

Lede paragraph:
  margin-top: 32px (--space-5), max-width: 520px
  font: sans 17px/400 (--text-body-lg), line-height 1.55, color fgSoft
  "Daniel Trochez" — weight 700, color fg

CTA row:
  margin-top: 32px (--space-5), display: flex, gap: 32px (--space-5), flex-wrap: wrap
  Primary: <a href="#contact"> — orange bg, #120a05/#fff, mono 12/700 (--text-mono), padding 16px 24px (--space-3/--space-4), radius 8 (--radius-lg)
  Secondary: <a href="#work"> — borderStrong border, mono 12/700 (--text-mono), color fg, padding 16px 24px (--space-3/--space-4), radius 8
    "↓" suffix — color: orangeText
```

### Hero Right Column — Terminal Card

```
Container:
  border: 1px var(--ms-border-strong)
  border-radius: 10px (--radius-xl)
  bg: --ms-surface
  font: mono
  overflow: hidden

Title bar:
  padding: 24px 16px (--space-4 top/bottom, --space-3 left/right)
  border-bottom: 1px var(--ms-border)
  display: flex, align-center, gap: 8px (--space-2)
  [traffic lights × 3, aria-hidden, gap: 4px (--space-1)]
  "~ / status.sh" — label 11/400 (--text-label), color fgFaint, margin-left: 24px (--space-4)

Body:
  padding: 48px (--space-6)
  line-height: 1.75
  font: mono 12/400 (--text-mono)
  color: fg

Terminal lines:
  "$ whoami" — $ in fgFaint
  "> daniel.trochez" — color: orangeText, margin-bottom: 16px
  "$ cat role"
  "→ software_developer" — margin-bottom: 16px
  "$ ./availability"
  "● open · 2–3 slots left" — ● in orangeText, margin-bottom: 16px
  "$ stack --short"
  Stack chips row: flex, flex-wrap, gap: 4px (--space-1), margin-top: 8px (--space-2)
    Each chip: padding 2px 8px, border: 1px var(--ms-border), radius 4 (--radius-sm),
    label 11/400 (--text-label), fgSoft
    6 chips: TS · C# · Swift · React · .NET · Node
  Cursor line: margin-top: 24px (--space-4), color fgFaint
    "$ " + cursor span:
      border-right: 7px solid var(--ms-orange)
      padding-right: 1px
      animation: ms-cursor 1.1s step-end infinite
      Content: "_"
```

### Trust Strip

```
Container:
  margin-top: 64px (--space-7)
  padding-top: 32px (--space-5)
  border-top: 1px var(--ms-border)
  display: grid
  grid-template-columns: repeat(4, 1fr)
  gap: 32px (--space-5)

Each stat:
  Number row: sans 17px/400 (--text-body-lg), tracking -0.03em, color fg, line-height 1
    "+" and "%" chars: color orangeText (span wrapper)
  Label row: margin-top: 8px (--space-2), label 11/400 (--text-label), color fgSoft, letter-spacing 0.4px
    Sub-text (e.g. "(on time)"): color fgFaint

Stats (in order):
  "08+" · "years shipping" · (no sub)
  "40+" · "projects delivered" · (no sub)
  "07" · "languages in stack" · (no sub)
  "100%" · "projects shipped" · "(on time)"
```

---

## Animation Contracts (Phase 2 active animations)

All animation keyframes and durations are pre-declared in Phase 1. Phase 2 activates
two of them for the first time in rendered components:

| Animation | Keyframe | Used by | Reduced-motion |
|-----------|----------|---------|----------------|
| `ms-pulse` | `scale 1→2.6, opacity 0.5→0, 2.2s ease-out infinite` | Status dot (hero + hire CTA) | `animation: none` |
| `ms-cursor` | `opacity 1→0, step-end, 1.1s infinite` | Terminal cursor `_` | `animation: none` |

Reduced-motion rule is already declared in `globals.css` from Phase 1:
```css
@media (prefers-reduced-motion: reduce) {
  .animate-pulse, [class*="ms-cursor"] { animation: none; }
}
```

Executor must ensure the cursor span's class name contains "ms-cursor" OR add a
matching class/keyframe name so the reduced-motion rule fires correctly.

Hover transitions (no keyframes — CSS `transition` only):
- All buttons: `transition: background-color 0.2s` (via `--anim-hover`)
- Theme toggle buttons: `transition: background-color 0.2s, color 0.2s`

---

## Component Inventory

### New components to create in this phase

| Component | File | Type | Description |
|-----------|------|------|-------------|
| `SiteHeader` | `components/SiteHeader.tsx` | `"use client"` | Sticky header with logo, nav, theme toggle, hire CTA |
| `HeroSection` | `components/HeroSection.tsx` | server component | Hero H1, lede, CTAs, terminal card, trust strip |
| `StatusDot` | `components/ui/StatusDot.tsx` | server component | 8×8 orange dot, optional pulse ring |
| `TerminalCard` | `components/TerminalCard.tsx` | server component | Terminal status card with cursor animation |

### Reuse from Phase 1

| Component | File | Notes |
|-----------|------|-------|
| `ThemeToggle` | `components/ThemeToggle.tsx` | Logic stays identical — markup replaced with pill style integrated into `SiteHeader` |

### shadcn components (no new installs required)

No new shadcn components needed for Phase 2. Existing `button` and `badge` from Phase 1
are not used directly — the header/hero CTAs use custom `<a>` and `<button>` elements
styled with design tokens per handoff spec.

---

## Accessibility Contract

| Element | Requirement |
|---------|-------------|
| Logo link `<a href="#top">` | No aria-label needed — contains visible text "monkey/solutions" |
| Nav links `<a href="#about">` etc. | No aria-label needed — number + label are visible text |
| Theme toggle buttons | Each `<button>` needs `aria-label="Switch to dark theme"` / `aria-label="Switch to light theme"`. The `☾` and `☀` symbols are decorative — wrap in `aria-hidden` span + explicit label. |
| Hire CTA `<a href="#contact">` | No aria-label needed — "Hire me" is clear link text |
| Decorative grid bg | `aria-hidden="true"` — see handoff |
| Orange glow blob | `aria-hidden="true"` |
| Traffic-light dots in terminal | `aria-hidden="true"` |
| Status dot (hero) | `aria-hidden="true"` — pulse is decorative; availability info is in adjacent text |
| Focus ring | All interactive elements: `focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2` (uses `hsl(var(--ring))` = orange) |
| Hero H1 | Plain `<h1>` — screen readers read it in natural order |
| Terminal card | Wrap in a `<div>` — NOT a `<section>` or `<article>`. It is decorative. |

---

## Semantic HTML Structure

```html
<header> <!-- sticky, SiteHeader -->
  <div class="inner"> <!-- max-w 1240px -->
    <a href="#top"> <!-- logo -->
    <nav> <!-- numbered links + toggle + CTA -->
  </div>
</header>

<main>
  <section id="top"> <!-- hero, HeroSection -->
    <!-- aria-hidden decorative layers -->
    <div class="inner"> <!-- max-w 1240px -->
      <div class="hero-grid"> <!-- 1.4fr 1fr -->
        <div class="hero-left"> <!-- H1 + lede + CTAs -->
        <div class="hero-right"> <!-- TerminalCard -->
      </div>
      <div class="trust-strip"> <!-- 4 stats -->
    </div>
  </section>
  <!-- Subsequent sections added in Phase 3+ -->
</main>
```

`<main>` tag is introduced in Phase 2. Existing `page.tsx` wraps content in a `<main>` — confirm this is already in place; if not, add it.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Hero status row | "AVAILABLE · freelance projects, Q2 2026 →" |
| Hero H1 | "Software that ships & lasts." |
| Hero lede | "I'm **Daniel Trochez** — a Gothenburg-based software developer running Monkey Solutions. I help teams and founders turn messy briefs into quietly well-made web, iOS & backend products." |
| Primary CTA | "Start a project →" |
| Secondary CTA | "View work ↓" |
| Hire CTA (header) | "Hire me" |
| Terminal — title bar | "~ / status.sh" |
| Terminal — whoami value | "daniel.trochez" |
| Terminal — role value | "software_developer" |
| Terminal — availability value | "open · 2–3 slots left" |
| Terminal — stack chips | "TS" · "C#" · "Swift" · "React" · ".NET" · "Node" |
| Trust stat 1 | "08+" / "years shipping" |
| Trust stat 2 | "40+" / "projects delivered" |
| Trust stat 3 | "07" / "languages in stack" |
| Trust stat 4 | "100%" / "projects shipped" / "(on time)" |
| Logo line 1 | "monkey/solutions" |
| Logo line 2 | "daniel_trochez.dev" |
| Nav links | "01 about" · "02 work" · "03 experience" · "04 skills" · "05 contact" |

Empty state: Not applicable — header and hero are always rendered.

Error state: Not applicable — this phase has no data fetching and no user input.

Destructive actions: None in this phase.

---

## Interaction Contract

| Interaction | Trigger | Response |
|-------------|---------|----------|
| Theme toggle | Click ☾ or ☀ button | Set `document.documentElement.dataset.theme`, persist to `localStorage("ms_theme")` |
| Logo click | Click `<a href="#top">` | Smooth scroll to top (`html { scroll-behavior: smooth }`) |
| Nav link click | Click any of 01–05 | Smooth scroll to `#about`, `#work`, `#experience`, `#skills`, `#contact` |
| Hire CTA click | Click `<a href="#contact">` | Smooth scroll to `#contact` section |
| Primary hero CTA click | Click "Start a project →" | Smooth scroll to `#contact` |
| Secondary hero CTA click | Click "View work ↓" | Smooth scroll to `#work` |
| Status dot | Continuous | Pulse animation `ms-pulse 2.2s ease-out infinite` |
| Terminal cursor | Continuous | Blink animation `ms-cursor 1.1s step-end infinite` |
| Header scroll behavior | Page scrolled | Header stays fixed at top; backdrop blur prevents overlap with content |
| ThemeToggle temp button | — | Remove from `app/page.tsx` (was Phase 1 temp placement) |

---

## Phase 1 Cleanup Required

The temporary ThemeToggle in `app/page.tsx` (fixed-position button placed for Phase 1
verification) must be removed in this phase. The toggle moves into `SiteHeader`.

---

## Border Radius — Phase 2 Usage

| Element | Token | Value |
|---------|-------|-------|
| Logo "M" square | `--radius-md` | 6px |
| Hire CTA button | `--radius-md` | 6px |
| Primary hero CTA | `--radius-lg` | 8px |
| Secondary hero CTA | `--radius-lg` | 8px |
| Terminal card | `--radius-xl` | 10px |
| Theme toggle container | `border-radius: 20px` | hardcoded — pill shape |
| Theme toggle active button | `border-radius: 18px` | hardcoded — inner pill |
| Terminal stack chips | `--radius-sm` | 4px |
| Traffic light dots | `border-radius: 5px` | 10×10 circle |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official (`https://ui.shadcn.com/r/...`) | badge, button, card (already installed from Phase 1) | Not required — official registry |
| Third-party | None declared | Not applicable |

No new shadcn components are installed in Phase 2. All UI is built from design
tokens, Tailwind utilities, and custom components per the handoff spec.

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: PASS
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS
- [x] Dimension 5 Spacing: PASS
- [x] Dimension 6 Registry Safety: PASS

**Approval:** approved (2026-05-09)
