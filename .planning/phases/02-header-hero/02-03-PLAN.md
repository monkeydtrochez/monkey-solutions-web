---
phase: 02-header-hero
plan: 03
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - components/HeroSection.tsx
  - components/TerminalCard.tsx
autonomous: true
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - HERO-04
tags:
  - react
  - css-tokens
  - animation
  - typography
must_haves:
  truths:
    - "User sees a hero section with a status row containing a pulsing dot and 'AVAILABLE · freelance projects, Q2 2026 →' text"
    - "User sees an H1 reading 'Software that ships & lasts.' with mixed weights (400 base, 700 for 'ships' and 'lasts.') and a Fraunces italic '&' accent in orange"
    - "If profile.heroBio is set in Sanity, the lede paragraph renders with 'Daniel Trochez' bolded; if heroBio is null/empty, no lede paragraph renders (D-02)"
    - "User sees a primary 'Start a project →' CTA linking to #contact and a secondary 'View work ↓' CTA linking to #work"
    - "User sees a terminal status card with three traffic-light dots, the title '~ / status.sh', whoami/role/availability/stack lines, six stack chips, and a blinking cursor"
    - "User sees a trust strip with 4 stats below the hero grid: 08+ years shipping, 40+ projects delivered, 07 languages in stack, 100% projects shipped (on time)"
    - "Cursor blink uses the ms-cursor keyframe and a className containing 'ms-cursor' so prefers-reduced-motion disables it"
  artifacts:
    - path: "components/HeroSection.tsx"
      provides: "Full hero section: status row, H1, lede, CTA row, terminal column, trust strip"
      contains: "Software that"
      min_lines: 150
    - path: "components/TerminalCard.tsx"
      provides: "Terminal window with traffic lights, body lines, stack chips, blinking cursor"
      contains: "ms-cursor"
      min_lines: 60
  key_links:
    - from: "components/HeroSection.tsx"
      to: "app/context/GlobalContext.tsx"
      via: "useContext(GlobalContext) -> profile?.heroBio (D-02)"
      pattern: "profile\\?\\.heroBio"
    - from: "components/HeroSection.tsx"
      to: "components/TerminalCard.tsx"
      via: "import + render as right column"
      pattern: "TerminalCard"
    - from: "components/HeroSection.tsx"
      to: "components/ui/StatusDot.tsx"
      via: "import + render in status row"
      pattern: "StatusDot"
    - from: "components/TerminalCard.tsx"
      to: "app/globals.css ms-cursor keyframe"
      via: "className='ms-cursor' + animation: ms-cursor"
      pattern: "ms-cursor"
---

<objective>
Build the full `HeroSection` (replacing the Plan 01 stub) and the `TerminalCard` it embeds. The hero is the primary above-the-fold element: status row + mixed-weight H1 + optional CMS-backed lede + two CTAs + terminal status card on the right + 4-stat trust strip below the grid. The cursor blink animation uses the existing `ms-cursor` keyframe from globals.css; the className must contain `"ms-cursor"` so the existing reduced-motion rule fires.

Purpose: Implements HERO-01 (mixed-weight H1 with Fraunces italic accent), HERO-02 (terminal card with cursor blink), HERO-03 (primary + secondary CTAs), HERO-04 (4-stat trust strip with orange accent characters). Also implements D-02 (heroBio from CMS) and D-03 (all other hero copy hardcoded).

Output: A complete hero section visible below the header on `localhost:3000`; `npm run build` green; visual matches UI-SPEC §Hero Section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-header-hero/02-CONTEXT.md
@.planning/phases/02-header-hero/02-RESEARCH.md
@.planning/phases/02-header-hero/02-PATTERNS.md
@.planning/phases/02-header-hero/02-UI-SPEC.md

<interfaces>
<!-- From plan 02-01 (now in place) -->

components/HeroSection.tsx (current — stub from plan 01):
```tsx
"use client";
export default function HeroSection() {
  return null;
}
```

components/TerminalCard.tsx (current — stub from plan 01, server component):
```tsx
export default function TerminalCard() {
  return null;
}
```

components/ui/StatusDot.tsx (filled in by plan 02 in same wave; stub remains usable if plan 02 hasn't merged yet):
```tsx
export default function StatusDot(props?: { pulse?: boolean }): JSX.Element
```
The default-exported component renders a pulsing 8x8 orange dot wrapped in `aria-hidden`. Default `pulse=true`.

app/context/GlobalContext.tsx (after plan 01 slim-down):
```typescript
interface ContextType {
  profile: Profile | null;
  // ... profile.heroBio?: string is the field used here
  setSiteContentToContext: (data: SanityApiResponse[]) => void;
}
```

app/globals.css existing tokens used by this plan:
- Colors: --ms-bg, --ms-fg, --ms-fg-soft, --ms-fg-faint, --ms-orange, --ms-orange-text, --ms-border, --ms-border-strong, --ms-surface
- Typography: --text-hero, --text-body-lg, --text-mono, --text-label, --lh-tight, --tracking-tight
- Fonts: --font-sans, --font-mono, --font-display
- Radii: --radius-sm, --radius-md, --radius-lg, --radius-xl
- Animations: --anim-cursor (= 1.1s)
- Traffic lights: --color-tl-red, --color-tl-yellow, --color-tl-green (consumed via hsl(var(--color-tl-red)))

Existing keyframes in globals.css (DO NOT redefine):
```css
@keyframes ms-cursor {
  0%, 49% { opacity: 1; }
  50%, 100% { opacity: 0; }
}
@media (prefers-reduced-motion: reduce) {
  .animate-pulse, [class*="ms-cursor"] { animation: none; }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build TerminalCard (HERO-02)</name>
  <files>components/TerminalCard.tsx</files>
  <read_first>
    - components/TerminalCard.tsx (current stub from plan 01)
    - app/globals.css (verify ms-cursor keyframe and reduced-motion rule)
    - .planning/phases/02-header-hero/02-UI-SPEC.md (Hero Right Column — Terminal Card section, Copywriting Contract — terminal lines, Color — Terminal Traffic Lights)
    - .planning/phases/02-header-hero/02-RESEARCH.md (Pattern 4: Cursor Blink Animation)
    - .planning/phases/02-header-hero/02-PATTERNS.md (TerminalCard pattern, lines 219–262)
  </read_first>
  <action>
    Replace the entire contents of `components/TerminalCard.tsx`. Server component — do NOT add `"use client"`.

    Full file:
    ```tsx
    const STACK_CHIPS = ["TS", "C#", "Swift", "React", ".NET", "Node"];

    export default function TerminalCard() {
      return (
        <div
          style={{
            background: "var(--ms-surface)",
            border: "1px solid var(--ms-border-strong)",
            borderRadius: "var(--radius-xl)",
            fontFamily: "var(--font-mono)",
            overflow: "hidden",
          }}
        >
          {/* Title bar */}
          <div
            style={{
              display: "flex",
              alignItems: "center",
              gap: 8,
              padding: "10px 16px",
              borderBottom: "1px solid var(--ms-border)",
            }}
          >
            <span
              aria-hidden="true"
              style={{ display: "inline-flex", gap: 4 }}
            >
              <span
                style={{
                  width: 10,
                  height: 10,
                  borderRadius: 5,
                  background: "hsl(var(--color-tl-red))",
                }}
              />
              <span
                style={{
                  width: 10,
                  height: 10,
                  borderRadius: 5,
                  background: "hsl(var(--color-tl-yellow))",
                }}
              />
              <span
                style={{
                  width: 10,
                  height: 10,
                  borderRadius: 5,
                  background: "hsl(var(--color-tl-green))",
                }}
              />
            </span>
            <span
              style={{
                marginLeft: 24,
                fontSize: "var(--text-label)",
                fontWeight: 400,
                color: "var(--ms-fg-faint)",
              }}
            >
              ~ / status.sh
            </span>
          </div>

          {/* Body */}
          <div
            style={{
              padding: 32,
              fontSize: "var(--text-mono)",
              fontWeight: 400,
              lineHeight: 1.75,
              color: "var(--ms-fg)",
            }}
          >
            <div>
              <span style={{ color: "var(--ms-fg-faint)" }}>$</span> whoami
            </div>
            <div
              style={{
                color: "var(--ms-orange-text)",
                marginBottom: 16,
              }}
            >
              &gt; daniel.trochez
            </div>

            <div>
              <span style={{ color: "var(--ms-fg-faint)" }}>$</span> cat role
            </div>
            <div style={{ marginBottom: 16 }}>→ software_developer</div>

            <div>
              <span style={{ color: "var(--ms-fg-faint)" }}>$</span>{" "}
              ./availability
            </div>
            <div style={{ marginBottom: 16 }}>
              <span style={{ color: "var(--ms-orange-text)" }}>●</span> open ·
              2–3 slots left
            </div>

            <div>
              <span style={{ color: "var(--ms-fg-faint)" }}>$</span> stack
              --short
            </div>
            <div
              style={{
                display: "flex",
                flexWrap: "wrap",
                gap: 4,
                marginTop: 8,
              }}
            >
              {STACK_CHIPS.map((chip) => (
                <span
                  key={chip}
                  style={{
                    padding: "2px 8px",
                    border: "1px solid var(--ms-border)",
                    borderRadius: "var(--radius-sm)",
                    fontSize: "var(--text-label)",
                    fontWeight: 400,
                    color: "var(--ms-fg-soft)",
                  }}
                >
                  {chip}
                </span>
              ))}
            </div>

            <div
              style={{
                marginTop: 24,
                color: "var(--ms-fg-faint)",
              }}
            >
              ${" "}
              <span
                className="ms-cursor"
                aria-hidden="true"
                style={{
                  borderRight: "7px solid var(--ms-orange)",
                  paddingRight: 1,
                  animation: "ms-cursor var(--anim-cursor) step-end infinite",
                }}
              >
                _
              </span>
            </div>
          </div>
        </div>
      );
    }
    ```

    Critical correctness notes:
    - Server component — NO `"use client"` directive.
    - Cursor span MUST have `className="ms-cursor"` (with the substring `ms-cursor`) so the existing reduced-motion rule `[class*="ms-cursor"] { animation: none; }` fires. Without this exact className, reduced-motion users still see the blink animation. Researched in 02-RESEARCH.md "Pattern 4".
    - Traffic light colors are consumed via `hsl(var(--color-tl-red))` etc. — globals.css declares these as raw HSL component values (e.g. `3 100% 67%`), so the `hsl()` wrapper is required.
    - Stack chips array contains exactly the 6 entries from UI-SPEC §Copywriting Contract: `TS, C#, Swift, React, .NET, Node` — DO NOT change order or values.
    - The `~ / status.sh` text uses `var(--text-label)` (11px) per UI-SPEC §Typography element mapping.
    - Body text uses `var(--text-mono)` (12px), line-height 1.75 per UI-SPEC.
    - Cursor border-right is `7px solid var(--ms-orange)` per UI-SPEC §Hero Right Column — Terminal Card "Cursor line".
    - Per UI-SPEC §Spacing, terminal body padding is 48px in the original spec but UI-SPEC notes spacing tokens — using `padding: 32` (literal pixels) here is the simpler, mismatch-safe choice. Both 32 and 48 fall within the standard set; 32 is chosen to match the dense terminal feel and to share the standard spacing rhythm with hero column gaps.
    - Use `&gt;` (HTML entity) for the `>` inside JSX text content to avoid React's tag interpretation.
  </action>
  <verify>
    <automated>grep -q "export default function TerminalCard" components/TerminalCard.tsx && grep -q 'className="ms-cursor"' components/TerminalCard.tsx && grep -q "ms-cursor var(--anim-cursor)" components/TerminalCard.tsx && grep -q "status.sh" components/TerminalCard.tsx && ! grep -q '"use client"' components/TerminalCard.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export default function TerminalCard" components/TerminalCard.tsx` outputs `1`
    - `grep -c '"use client"' components/TerminalCard.tsx` outputs `0` (server component)
    - `grep -c 'className="ms-cursor"' components/TerminalCard.tsx` outputs `1` (reduced-motion gate)
    - `grep -c "ms-cursor var(--anim-cursor) step-end infinite" components/TerminalCard.tsx` outputs `1`
    - `grep -c "borderRight: \"7px solid var(--ms-orange)\"" components/TerminalCard.tsx` outputs `1`
    - `grep -c "~ / status.sh" components/TerminalCard.tsx` outputs `1`
    - `grep -c "daniel.trochez" components/TerminalCard.tsx` outputs `1`
    - `grep -c "software_developer" components/TerminalCard.tsx` outputs `1`
    - `grep -c "open · 2–3 slots left" components/TerminalCard.tsx` outputs `1`
    - `grep -c '"TS"' components/TerminalCard.tsx` outputs `1`
    - `grep -c '"C#"' components/TerminalCard.tsx` outputs `1`
    - `grep -c '"Swift"' components/TerminalCard.tsx` outputs `1`
    - `grep -c '"React"' components/TerminalCard.tsx` outputs `1`
    - `grep -c '".NET"' components/TerminalCard.tsx` outputs `1`
    - `grep -c '"Node"' components/TerminalCard.tsx` outputs `1`
    - `grep -c "hsl(var(--color-tl-red))" components/TerminalCard.tsx` outputs `1`
    - `grep -c "hsl(var(--color-tl-yellow))" components/TerminalCard.tsx` outputs `1`
    - `grep -c "hsl(var(--color-tl-green))" components/TerminalCard.tsx` outputs `1`
    - `grep -c 'aria-hidden="true"' components/TerminalCard.tsx` outputs at least `2` (traffic-light wrapper + cursor span)
    - `wc -l < components/TerminalCard.tsx` outputs at least `60`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>TerminalCard renders a complete terminal-styled card with traffic lights, four labeled lines, six stack chips, and a blinking cursor that respects prefers-reduced-motion.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build HeroSection (HERO-01, HERO-02, HERO-03, HERO-04, D-02, D-03)</name>
  <files>components/HeroSection.tsx</files>
  <read_first>
    - components/HeroSection.tsx (current stub from plan 01)
    - components/TerminalCard.tsx (after Task 1 — verify default export)
    - components/ui/StatusDot.tsx (after plan 02-02 OR stub from 02-01 — default export with optional `pulse` prop)
    - app/context/GlobalContext.tsx (after plan 01 — slim ContextType, profile.heroBio)
    - app/models/sanityTypes.ts (after plan 01 — Profile.heroBio?: string)
    - .planning/phases/02-header-hero/02-UI-SPEC.md (Hero Section, Hero Left Column, Trust Strip, Copywriting Contract)
    - .planning/phases/02-header-hero/02-CONTEXT.md (D-02 lede null/empty rule, D-03 hardcoded copy list)
    - .planning/phases/02-header-hero/02-RESEARCH.md (Pitfall 2: HeroSection client component, Pitfall 5: heroBio race)
    - .planning/phases/02-header-hero/02-PATTERNS.md (HeroSection pattern lines 139–215)
  </read_first>
  <action>
    Replace the entire contents of `components/HeroSection.tsx`. Must be `"use client"` because it reads `GlobalContext` via `useContext` to get `profile.heroBio` (D-02).

    Full file:
    ```tsx
    "use client";
    import { useContext } from "react";
    import GlobalContext from "@/app/context/GlobalContext";
    import StatusDot from "@/components/ui/StatusDot";
    import TerminalCard from "@/components/TerminalCard";

    type TrustStat = {
      number: string;
      accent?: "prefix" | "suffix";
      accentChar?: string;
      label: string;
      sub?: string;
    };

    const TRUST_STATS: TrustStat[] = [
      { number: "08", accent: "suffix", accentChar: "+", label: "years shipping" },
      { number: "40", accent: "suffix", accentChar: "+", label: "projects delivered" },
      { number: "07", label: "languages in stack" },
      { number: "100", accent: "suffix", accentChar: "%", label: "projects shipped", sub: "(on time)" },
    ];

    export default function HeroSection() {
      const ctx = useContext(GlobalContext);
      const profile = ctx?.profile ?? null;
      const heroBio = profile?.heroBio?.trim();

      return (
        <section
          id="top"
          style={{
            position: "relative",
            overflow: "hidden",
            padding: "64px 32px",
            background: "var(--ms-bg)",
          }}
        >
          {/* Decorative grid background */}
          <div
            aria-hidden="true"
            style={{
              position: "absolute",
              inset: 0,
              backgroundImage:
                "repeating-linear-gradient(0deg, var(--ms-border) 0 1px, transparent 1px 64px), repeating-linear-gradient(90deg, var(--ms-border) 0 1px, transparent 1px 64px)",
              maskImage:
                "radial-gradient(ellipse at 70% 40%, #000 20%, transparent 70%)",
              WebkitMaskImage:
                "radial-gradient(ellipse at 70% 40%, #000 20%, transparent 70%)",
              opacity: 0.5,
              pointerEvents: "none",
            }}
          />
          {/* Decorative orange glow blob */}
          <div
            aria-hidden="true"
            style={{
              position: "absolute",
              right: "-10%",
              top: "10%",
              width: 520,
              height: 520,
              background:
                "radial-gradient(circle, color-mix(in srgb, var(--ms-orange) 13%, transparent) 0%, transparent 60%)",
              pointerEvents: "none",
            }}
          />

          <div
            style={{
              position: "relative",
              maxWidth: "var(--content-max)",
              margin: "0 auto",
            }}
          >
            <div
              style={{
                display: "grid",
                gridTemplateColumns: "1.4fr 1fr",
                gap: 64,
                alignItems: "end",
              }}
            >
              {/* Left column */}
              <div>
                {/* Status row */}
                <div
                  style={{
                    display: "flex",
                    alignItems: "center",
                    gap: 24,
                    marginBottom: 32,
                    fontFamily: "var(--font-mono)",
                    fontSize: "var(--text-mono)",
                    fontWeight: 400,
                    color: "var(--ms-fg-soft)",
                  }}
                >
                  <StatusDot />
                  <span style={{ color: "var(--ms-orange-text)", fontWeight: 700 }}>
                    AVAILABLE
                  </span>
                  <span>· freelance projects, Q2 2026 →</span>
                </div>

                {/* H1 */}
                <h1
                  style={{
                    fontFamily: "var(--font-sans)",
                    fontSize: "var(--text-hero)",
                    fontWeight: 400,
                    lineHeight: "var(--lh-tight)",
                    letterSpacing: "var(--tracking-tight)",
                    color: "var(--ms-fg)",
                    margin: 0,
                  }}
                >
                  Software that <span style={{ fontWeight: 700 }}>ships</span>{" "}
                  <em
                    style={{
                      fontFamily: "var(--font-display)",
                      fontStyle: "italic",
                      fontWeight: 400,
                      color: "var(--ms-orange-text)",
                    }}
                  >
                    &amp;
                  </em>{" "}
                  <span style={{ fontWeight: 700 }}>lasts.</span>
                </h1>

                {/* Lede paragraph (D-02: render only if heroBio truthy) */}
                {heroBio && (
                  <p
                    style={{
                      marginTop: 32,
                      maxWidth: 520,
                      fontFamily: "var(--font-sans)",
                      fontSize: "var(--text-body-lg)",
                      fontWeight: 400,
                      lineHeight: 1.55,
                      color: "var(--ms-fg-soft)",
                    }}
                  >
                    {heroBio}
                  </p>
                )}

                {/* CTA row */}
                <div
                  style={{
                    marginTop: 32,
                    display: "flex",
                    gap: 32,
                    flexWrap: "wrap",
                  }}
                >
                  <a
                    href="#contact"
                    style={{
                      display: "inline-flex",
                      alignItems: "center",
                      gap: 8,
                      background: "var(--ms-orange)",
                      color: "#120a05",
                      padding: "16px 24px",
                      borderRadius: "var(--radius-lg)",
                      fontFamily: "var(--font-mono)",
                      fontSize: "var(--text-mono)",
                      fontWeight: 700,
                      textDecoration: "none",
                      transition: "opacity var(--anim-hover)",
                    }}
                  >
                    Start a project →
                  </a>
                  <a
                    href="#work"
                    style={{
                      display: "inline-flex",
                      alignItems: "center",
                      gap: 8,
                      border: "1px solid var(--ms-border-strong)",
                      color: "var(--ms-fg)",
                      padding: "16px 24px",
                      borderRadius: "var(--radius-lg)",
                      fontFamily: "var(--font-mono)",
                      fontSize: "var(--text-mono)",
                      fontWeight: 700,
                      textDecoration: "none",
                      transition: "border-color var(--anim-hover)",
                    }}
                  >
                    View work{" "}
                    <span style={{ color: "var(--ms-orange-text)" }}>↓</span>
                  </a>
                </div>
              </div>

              {/* Right column */}
              <div>
                <TerminalCard />
              </div>
            </div>

            {/* Trust strip */}
            <div
              style={{
                marginTop: 64,
                paddingTop: 32,
                borderTop: "1px solid var(--ms-border)",
                display: "grid",
                gridTemplateColumns: "repeat(4, 1fr)",
                gap: 32,
              }}
            >
              {TRUST_STATS.map((stat) => (
                <div key={stat.label}>
                  <div
                    style={{
                      fontFamily: "var(--font-sans)",
                      fontSize: "var(--text-body-lg)",
                      fontWeight: 400,
                      letterSpacing: "-0.03em",
                      lineHeight: 1,
                      color: "var(--ms-fg)",
                    }}
                  >
                    {stat.accent === "prefix" && stat.accentChar && (
                      <span style={{ color: "var(--ms-orange-text)" }}>
                        {stat.accentChar}
                      </span>
                    )}
                    {stat.number}
                    {stat.accent === "suffix" && stat.accentChar && (
                      <span style={{ color: "var(--ms-orange-text)" }}>
                        {stat.accentChar}
                      </span>
                    )}
                  </div>
                  <div
                    style={{
                      marginTop: 8,
                      fontFamily: "var(--font-mono)",
                      fontSize: "var(--text-label)",
                      fontWeight: 400,
                      color: "var(--ms-fg-soft)",
                      letterSpacing: "0.4px",
                    }}
                  >
                    {stat.label}
                    {stat.sub && (
                      <>
                        {" "}
                        <span style={{ color: "var(--ms-fg-faint)" }}>
                          {stat.sub}
                        </span>
                      </>
                    )}
                  </div>
                </div>
              ))}
            </div>
          </div>
        </section>
      );
    }
    ```

    Implementation notes (concrete values, no "match X to Y"):
    - **D-02 (lede null/empty rule):** `const heroBio = profile?.heroBio?.trim();` then `{heroBio && ( ... )}`. An empty string after trim is falsy. No fallback string.
    - **D-03 (hardcoded copy):** Status row "AVAILABLE · freelance projects, Q2 2026 →"; H1 "Software that ships & lasts."; CTAs "Start a project →" / "View work ↓"; trust stats "08+", "40+", "07", "100%". All exact strings from UI-SPEC §Copywriting Contract.
    - **HERO-01 (mixed weights):** H1 wraps "ships" and "lasts." in `<span style={{ fontWeight: 700 }}>`; "Software that" remains weight 400; the "&" uses `<em>` with `fontFamily: "var(--font-display)"`, `fontStyle: "italic"`, `fontWeight: 400`, `color: "var(--ms-orange-text)"`. Use the JSX entity `&amp;` (NOT `&`) so React doesn't choke.
    - **HERO-02 (terminal):** rendered via `<TerminalCard />` in the right grid column.
    - **HERO-03 (CTAs):** primary = `<a href="#contact">` orange button; secondary = `<a href="#work">` outlined button with orange `↓` suffix.
    - **HERO-04 (trust strip):** 4 stats in a grid, accent characters wrapped in `<span style={{ color: "var(--ms-orange-text)" }}>`.
    - The `id="top"` on the section satisfies the logo's `<a href="#top">` anchor.
    - `heroBio?.trim()` guards against whitespace-only Sanity values being treated as truthy.
    - Per Pitfall 5: heroBio race on first render is acceptable per D-02 — the lede is conditional on the truthy check, so initial render with `null` heroBio shows nothing; once `DataHydrator` fires and React re-renders, the lede appears.
    - Per Pitfall 2: `"use client"` is required because we use `useContext`. HeroSection has no SSR-critical data anyway.
    - Decorative grid uses `repeating-linear-gradient` at 64px tile per UI-SPEC §Hero Background FX. Mask uses `radial-gradient(ellipse at 70% 40%, #000 20%, transparent 70%)`.
    - Decorative orange glow uses `color-mix(in srgb, var(--ms-orange) 13%, transparent)` per UI-SPEC implementation hint.
    - Both decorative layers are `aria-hidden="true"` and `pointerEvents: "none"`.
    - All gap/padding values use literal pixels (32, 64, 16, 24, 8) per UI-SPEC §Spacing standard set, intentionally avoiding the `--space-N` token name mismatch documented in research.
    - Hire CTA hardcoded text color `#120a05` matches Plan 02 SiteHeader hire CTA — the orange button uses near-black text in both themes.
  </action>
  <verify>
    <automated>grep -q "export default function HeroSection" components/HeroSection.tsx && grep -q '"use client"' components/HeroSection.tsx && grep -q "useContext(GlobalContext)" components/HeroSection.tsx && grep -q "profile?.heroBio" components/HeroSection.tsx && grep -q "Software that" components/HeroSection.tsx && grep -q "Start a project" components/HeroSection.tsx && grep -q "View work" components/HeroSection.tsx && grep -q "TerminalCard" components/HeroSection.tsx && grep -q 'id="top"' components/HeroSection.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - First non-empty line of file is `"use client";`
    - `grep -c "export default function HeroSection" components/HeroSection.tsx` outputs `1`
    - `grep -c "useContext(GlobalContext)" components/HeroSection.tsx` outputs `1`
    - `grep -c "profile?.heroBio" components/HeroSection.tsx` outputs at least `1`
    - `grep -c "heroBio &&" components/HeroSection.tsx` outputs `1` (D-02 conditional render)
    - `grep -c 'id="top"' components/HeroSection.tsx` outputs `1`
    - `grep -c "Software that" components/HeroSection.tsx` outputs `1`
    - `grep -c '>ships<' components/HeroSection.tsx` outputs `1`
    - `grep -c '>lasts.<' components/HeroSection.tsx` outputs `1`
    - `grep -c "&amp;" components/HeroSection.tsx` outputs `1`
    - `grep -c 'fontFamily: "var(--font-display)"' components/HeroSection.tsx` outputs `1`
    - `grep -c 'fontStyle: "italic"' components/HeroSection.tsx` outputs `1`
    - `grep -c "AVAILABLE" components/HeroSection.tsx` outputs `1`
    - `grep -c "freelance projects, Q2 2026" components/HeroSection.tsx` outputs `1`
    - `grep -c "Start a project →" components/HeroSection.tsx` outputs `1`
    - `grep -c "View work" components/HeroSection.tsx` outputs `1`
    - `grep -c 'href="#contact"' components/HeroSection.tsx` outputs `1`
    - `grep -c 'href="#work"' components/HeroSection.tsx` outputs `1`
    - `grep -c '"08"' components/HeroSection.tsx` outputs `1`
    - `grep -c '"40"' components/HeroSection.tsx` outputs `1`
    - `grep -c '"07"' components/HeroSection.tsx` outputs `1`
    - `grep -c '"100"' components/HeroSection.tsx` outputs `1`
    - `grep -c "years shipping" components/HeroSection.tsx` outputs `1`
    - `grep -c "projects delivered" components/HeroSection.tsx` outputs `1`
    - `grep -c "languages in stack" components/HeroSection.tsx` outputs `1`
    - `grep -c "projects shipped" components/HeroSection.tsx` outputs `1`
    - `grep -c '(on time)' components/HeroSection.tsx` outputs `1`
    - `grep -c "<TerminalCard" components/HeroSection.tsx` outputs `1`
    - `grep -c "<StatusDot" components/HeroSection.tsx` outputs `1`
    - `grep -c "color-mix(in srgb, var(--ms-orange)" components/HeroSection.tsx` outputs `1`
    - `grep -c "repeating-linear-gradient" components/HeroSection.tsx` outputs `1`
    - `grep -c 'aria-hidden="true"' components/HeroSection.tsx` outputs at least `2` (grid + glow)
    - `grep -c "var(--text-hero)" components/HeroSection.tsx` outputs `1`
    - `grep -c "var(--text-body-lg)" components/HeroSection.tsx` outputs at least `2` (lede + trust numbers)
    - `wc -l < components/HeroSection.tsx` outputs at least `150`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>HeroSection renders the full UI-SPEC §Hero layout: status row with pulsing dot, mixed-weight H1 with Fraunces italic "&", optional Sanity-backed lede, two CTAs, terminal card on the right, and a 4-stat trust strip with orange accent characters. Decorative layers are aria-hidden.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sanity CMS heroBio → React text content | Plain string rendered via JSX `{heroBio}` — React auto-escapes. No `dangerouslySetInnerHTML`. |
| GlobalContext → HeroSection | In-process React context read; no network or storage boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Information Disclosure | profile.heroBio render | accept | Field is intentionally public profile content; no PII added. CMS auth controls who can edit. |
| T-02-08 | Tampering | profile.heroBio injection | mitigate | React auto-escapes string rendered as `{heroBio}`. No HTML/script execution path exists. |
| T-02-09 | Denial of Service | CSS animation (cursor blink) | accept | One CSS animation per page; reduced-motion users opt out via the existing globals.css rule. |

No high severity threats. No external APIs, no input fields, no auth.
</threat_model>

<verification>
- `npm run build` exits 0
- `npm run lint` passes
- Visiting `localhost:3000` after `npm run dev`:
  - Hero section renders below the header
  - H1 reads "Software that ships & lasts." with mixed weights and Fraunces italic "&"
  - Status row shows pulsing dot, orange "AVAILABLE", and the rest in fg-soft
  - Two CTAs are visible and clickable (links scroll within the page where IDs exist)
  - Terminal card on the right shows traffic lights, four labeled lines, six chips, blinking cursor
  - Trust strip shows 4 stats with orange "+" / "%" accent characters
  - If Sanity profile.heroBio is empty/null, the lede paragraph is absent (D-02)
  - With OS reduced-motion enabled, neither the dot nor cursor animates
</verification>

<success_criteria>
- HeroSection renders without errors when GlobalContext is null OR when heroBio is null/empty/whitespace
- HeroSection renders the lede paragraph when GlobalContext profile.heroBio is set to a non-empty string
- The H1 "&" character renders in Fraunces italic, orange-text color
- All four trust stats are present with correct numbers and labels
- Terminal cursor className contains `ms-cursor` so reduced-motion disables the blink
- `npm run build` is green
</success_criteria>

<output>
After completion, create `.planning/phases/02-header-hero/02-03-SUMMARY.md` per template.
</output>
