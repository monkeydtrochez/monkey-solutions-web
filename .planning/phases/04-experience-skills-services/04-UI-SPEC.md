---
phase: 4
slug: experience-skills-services
status: draft
shadcn_initialized: true
preset: "default / radix / lucide / v4"
created: 2026-05-10
---

# Phase 4 — UI Design Contract

> Visual and interaction contract for Phase 4: Experience, Skills + Services.
> Generated by gsd-ui-researcher.
>
> Phase 4 delivers three content sections: (1) an Experience section with a vertical
> timeline for 4 work entries + an education list + a community sub-section; (2) a
> Skills section with 4 labelled 10-segment bar groups; (3) a Services section with a
> 2×2 card grid. All design tokens, fonts, and layout patterns are pre-established by
> Phases 1–3 — this contract maps them to Phase 4 elements.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | shadcn (already initialized) | components.json detected |
| Style | default | `npx shadcn info` |
| Component base | radix | `npx shadcn info` |
| Icon library | lucide | `npx shadcn info` |
| Installed components | badge, button, card | `npx shadcn info` |
| Font — body/UI | Inter (variable, weight 300–800) | Phase 1 |
| Font — mono/kicker | JetBrains Mono (variable, weight 400–700) | Phase 1 |
| Font — display/editorial | Fraunces (variable italic, weight 300–500) | Phase 1 |
| CSS variable prefix | `--font-sans`, `--font-mono`, `--font-display` | Phase 1 complete |
| Theme strategy | `[data-theme="dark"]` / `[data-theme="light"]` on `<html>` | Phase 1 complete |
| Default theme | dark | Phase 1 complete |

No new shadcn components need to be installed. No third-party registries.

---

## Spacing Scale

All spacing tokens are pre-established in `globals.css :root` from Phase 1. This phase
uses these tokens by name. Do not add new spacing values — use only what is declared.

The Phase 1 token scale extends the base 8-point grid with 20, 28, and 36px values for
inter-element rhythm — this was established and approved in the Phase 1 design contract.
`--space-9` (20px), `--space-11` (28px), and `--space-13` (36px) are valid tokens from
that approved set and are not exceptions to the spacing scale.

`--section-py` (120px) and `--content-max` (1240px) are layout dimensions, not spacing
tokens, and are exempt from the spacing scale rule.

| Token | Value | Usage in this phase |
|-------|-------|---------------------|
| `--space-1` | 4px | Inner segment gap, detail line margin-top on education entry |
| `--space-3` | 8px | Timeline dot gap to text, community row gap between label and value, bar label gap |
| `--space-5` | 12px | Skill item rows gap (flex column gap between each row in a skill group) |
| `--space-7` | 16px | Skill group label margin-bottom, community row margin-top, education detail line margin-top |
| `--space-9` | 20px | Timeline entry column gap |
| `--space-10` | 24px | Skills group gap between groups, service card description margin-top |
| `--space-11` | 28px | Timeline entries gap (between rows) |
| `--space-12` | 32px | Education list margin-top, community section margin-top, service card padding |
| `--space-13` | 36px | Timeline section grid margin-top after kicker |
| `--space-15` | 48px | Section gap between Experience left and Skills/Services column blocks |
| `--content-max` | 1240px | Max content width for all three sections (layout dimension, not a spacing token) |
| `--page-px` | 32px | Horizontal section padding |
| `--section-py` | 120px | Vertical section padding for all three sections (layout dimension, not a spacing token) |

Exceptions:
- Timeline vertical line: `width: 1px` — not a spacing token; it is a border dimension.
- Timeline dot: `width: 12px; height: 12px` — hardcoded (not a spacing token). Current role dot: `16px × 16px` with orange pulse glow.
- Skills bar: each segment `width: calc((100% - Ngap) / 10)` where N = 9 gaps of 4px each. This is a proportional calculation, not a token application.
- Service card giant decorative number: `font-size: clamp(80px, 10vw, 140px)` — outside the type scale; decorative only.
- Timeline entry left column width: `20px` (dot + line) — hardcoded layout dimension.

---

## Typography

All type tokens are pre-declared in `globals.css :root` from Phase 1. This section maps
those tokens to exact element roles in Phase 4.

### Declared Type Tokens (4 — same vocabulary as Phases 2–3)

| Token | Rendered value | Font |
|-------|----------------|------|
| `--text-h2` | `clamp(36px, 5vw, 88px)` | Inter (`--font-sans`) |
| `--text-h3` | `26px` | Inter (`--font-sans`) — section sub-headings (Experience, Skills, Services) |
| `--text-body` | `16px` | Inter (`--font-sans`) |
| `--text-mono` | `12px` | JetBrains Mono (`--font-mono`) |

Exactly 4 sizes are declared. No other sizes are permitted. The decorative Fraunces giant
number on service cards does not count as a type role — it is aria-hidden decoration.

### Element Mapping

| Element | Size | Weight | Line Height | Additional |
|---------|------|--------|-------------|------------|
| Section H2 (03 kicker, "Experience + Education") | `--text-h2` | 400 / 400 italic mix | 1.02 | `--font-sans`, tracking `-0.035em`, color `--ms-fg` |
| H2 Fraunces italic accent ("Education") | `--text-h2` | 400 italic | 1.02 | `--font-display`, color `--ms-orange-text` |
| Sub-section heading ("Experience", "Skills", "Services") | `--text-h3` (26px) | 600 | 1.2 | `--font-sans`, tracking `-0.02em`, color `--ms-fg` |
| Kicker number (03, 04, 05) | `--text-mono` (12px) | 600 | n/a | `--font-mono`, color `--ms-orange-text` |
| Kicker label (EXPERIENCE + EDUCATION, SKILLS, SERVICES) | `--text-mono` (12px) | 400 | n/a | `--font-mono`, uppercase, letter-spacing 1 |
| Timeline — company name | `--text-body` (16px) | 600 | 1.3 | `--font-sans`, color `--ms-fg` |
| Timeline — role title | `--text-mono` (12px) | 400 | n/a | `--font-mono`, color `--ms-fg-soft` |
| Timeline — year range | `--text-mono` (12px) | 400 | n/a | `--font-mono`, color `--ms-fg-faint` |
| Timeline — description | `--text-body` (16px) | 400 | 1.6 | `--font-sans`, color `--ms-fg-soft`, max-width 480px |
| Timeline — current badge | `--text-mono` (12px) | 600 | n/a | `--font-mono`, color `--ms-orange-text`, uppercase |
| Education — degree title | `--text-body` (16px) | 600 | 1.3 | `--font-sans`, color `--ms-fg` |
| Education — institution | `--text-mono` (12px) | 400 | n/a | `--font-mono`, color `--ms-fg-soft` |
| Education — years | `--text-mono` (12px) | 400 | n/a | `--font-mono`, color `--ms-fg-faint`, text-align right |
| Education — detail line | `--text-mono` (12px) | 400 | n/a | `--font-mono`, color `--ms-fg-faint` |
| Community — activity label | `--text-mono` (12px) | 400 | n/a | `--font-mono`, color `--ms-fg-soft` |
| Community — activity detail | `--text-mono` (12px) | 400 | n/a | `--font-mono`, color `--ms-fg-faint` |
| Skill group name | `--text-mono` (12px) | 600 | n/a | `--font-mono`, uppercase, letter-spacing 1, color `--ms-fg` |
| Skill item label | `--text-body` (16px) | 400 | n/a | `--font-sans`, color `--ms-fg-soft` |
| Skill bar — proficiency number | `--text-mono` (12px) | 400 | n/a | `--font-mono`, color `--ms-fg-faint`, text-align right |
| Service card — number (decorative) | `clamp(80px, 10vw, 140px)` | 400 italic | 0.85 | `--font-display` (Fraunces), color `--ms-border-strong`, `aria-hidden="true"` |
| Service card — title | `--text-h3` (26px) | 600 | 1.2 | `--font-sans`, tracking `-0.02em`, color `--ms-fg` |
| Service card — description | `--text-body` (16px) | 400 | 1.6 | `--font-sans`, color `--ms-fg-soft` |
| Service card — stack chip | `--text-mono` (12px) | 400 | n/a | `--font-mono`, color `--ms-fg-soft` |

---

## Color

All color tokens are pre-established from Phase 1. This section declares which token maps
to which Phase 4 element.

60/30/10 split: dominant 60% `--ms-bg`, secondary 30% `--ms-bg-alt` + `--ms-surface`, accent 10% `--ms-orange` family.

| Role | Token | Dark value | Light value |
|------|-------|-----------|-------------|
| Dominant 60% — page bg | `--ms-bg` | `#0d0b09` | `#f7f4ee` |
| Secondary 30% — Experience + Education section bg | `--ms-bg-alt` | `#15120e` | `#efeae1` |
| Secondary 30% — Skills section bg | `--ms-bg` (same as page, no explicit bg class) | `#0d0b09` | `#f7f4ee` |
| Secondary 30% — Services section bg | `--ms-bg-alt` | `#15120e` | `#efeae1` |
| Secondary 30% — card/surface | `--ms-surface` | `#1a1612` | `#fdfcf8` |
| Accent 10% — orange | `--ms-orange` | `#ff6b1a` | `#e85a0f` |
| Orange text on dark surface | `--ms-orange-text` | `#ff8b42` | `#c94a05` |
| Orange dim tint | `--ms-orange-dim` | `rgba(255,107,26,0.15)` | `rgba(232,90,15,0.12)` |
| Orange mist | `--ms-mist` | `rgba(255,107,26,0.06)` | `rgba(232,90,15,0.04)` |
| Destructive | `--destructive` | `hsl(0 50% 30%)` | `hsl(0 50% 30%)` |

### Accent Reserved For (Phase 4 elements only)

- Kicker numbers ("03", "04", "05") — use `--ms-orange-text`
- H2 Fraunces italic accent ("Education") — use `--ms-orange-text`
- Timeline current-role pulse dot — use `--ms-orange` (dot) + `--ms-orange-dim` (pulse ring)
- Timeline current-role "Current" badge text — use `--ms-orange-text`
- Timeline vertical line segment for current role — use `--ms-orange-dim` (not `--ms-border`)
- Skills bar — filled segments — use `--ms-orange`
- Service card orange hover border — use `--ms-orange` (1px solid, `border-color: var(--ms-orange)`)
- `:focus-visible` ring on all interactive elements — use `hsl(var(--ring))` (= orange)

### Section Backgrounds

| Section | Background | Borders |
|---------|-----------|---------|
| Experience + Education (`#experience`) | `--ms-bg-alt` | `border-top`: 1px `--ms-border`; `border-bottom`: 1px `--ms-border` |
| Skills (`#skills`) | `--ms-bg` (default) | none |
| Services (`#services`) | `--ms-bg-alt` | `border-top`: 1px `--ms-border`; `border-bottom`: 1px `--ms-border` |

---

## Layout & Spacing — Element-Level Detail

### Section IDs and Nav Anchors

The header (`SiteHeader.tsx`) already outputs anchor links numbered 01–05. Phase 4 provides
anchor IDs that these links will eventually resolve to. Use IDs:
- `id="experience"` on the Experience + Education section
- `id="skills"` on the Skills section
- `id="services"` on the Services section

### Experience + Education Section

```
<section id="experience">
  padding: 120px 32px (--section-py / --page-px)
  background: --ms-bg-alt
  border-top: 1px var(--ms-border)
  border-bottom: 1px var(--ms-border)

  Inner: max-w 1240px, margin: 0 auto

  <Kicker n="03" label="Experience + Education" />
    flex, align-center, gap: 12px
    number: mono 12/600, --ms-orange-text
    line: 28×1px, bg --ms-border-strong
    label: mono 12/400, uppercase, letter-spacing 1, --ms-fg-soft

  H2 (section heading):
    margin-top: 36px
    font: sans clamp(36px, 5vw, 88px)/400 (--text-h2)
    tracking: -0.035em
    line-height: 1.02
    color: --ms-fg
    "Experience + "
    <span Fraunces italic 400 --ms-orange-text>Education</span>

  Two-column grid:
    margin-top: 36px
    display: grid
    grid-template-columns: 1fr 1fr
    gap: 64px (--space-17)
    align-items: start
```

### Experience Timeline (Left Column)

```
<div class="timeline">
  Vertical line:
    position: relative
    padding-left: 32px
    border-left: 1px var(--ms-border)

  For each entry (4 total):
    position: relative
    padding-bottom: 36px (--space-13)  [last entry: no padding-bottom]

    Dot:
      position: absolute
      left: -6px  (centers 12px dot over 1px line)
      top: 6px
      width: 12px; height: 12px
      border-radius: 50%
      background: --ms-border-strong (past roles)
      border: 1px var(--ms-bg-alt) [creates gap between dot and line]

    Current role dot overrides:
      width: 16px; height: 16px
      left: -8px; top: 4px
      background: --ms-orange
      box-shadow: 0 0 0 4px var(--ms-orange-dim)
      animation: ms-pulse (--anim-pulse 2.2s) infinite

    Entry content (flex column, gap: 4px --space-1):
      Company name: sans 16/600, --ms-fg
      Role title: mono 12/400, --ms-fg-soft
      Year range: mono 12/400, --ms-fg-faint
      Description: sans 16/400, line-height 1.6, --ms-fg-soft, max-width 480px, margin-top: 8px

      Current role "Current" badge:
        display: inline-block
        margin-left: 8px (next to company name on same line)
        mono 12/600, --ms-orange-text, uppercase
        padding: 4px 8px
        border: 1px var(--ms-orange-dim)
        border-radius: --radius-pill
```

### Education + Community (Right Column)

```
Education list:
  display: flex, flex-direction: column, gap: 24px (--space-10)

  Each entry (2 degrees):
    display: grid
    grid-template-columns: 1fr auto
    gap: 16px (--space-7)
    align-items: start

    Left:
      Degree title: sans 16/600, --ms-fg
      Institution: mono 12/400, --ms-fg-soft, margin-top: 4px (--space-1)
      Detail line: mono 12/400, --ms-fg-faint, margin-top: 8px (--space-3)

    Right:
      Years: mono 12/400, --ms-fg-faint, text-align right, white-space: nowrap

Community sub-section:
  margin-top: 48px (--space-15)

  Heading:
    mono 12/600, uppercase, letter-spacing 1, --ms-fg-faint, margin-bottom: 16px (--space-7)
    Label: "ALSO / COMMUNITY"

  Activity rows (3 rows):
    Each row:
      display: flex
      justify-content: space-between
      align-items: baseline
      gap: 16px (--space-7)
      padding: 12px 0
      border-bottom: 1px var(--ms-border)
      Activity label: mono 12/400, --ms-fg-soft
      Activity detail: mono 12/400, --ms-fg-faint, text-align right
```

### Skills Section

```
<section id="skills">
  padding: 120px 32px (--section-py / --page-px)
  background: --ms-bg (no explicit class needed)

  Inner: max-w 1240px, margin: 0 auto

  <Kicker n="04" label="Skills" />

  H2:
    margin-top: 36px
    "What I work with" (plain, no Fraunces accent in this section)
    font: sans clamp(36px, 5vw, 88px)/400, tracking -0.035em, line-height 1.02, --ms-fg

  4-column skills grid:
    margin-top: 48px (--space-15)
    display: grid
    grid-template-columns: repeat(4, 1fr)
    gap: 24px (--space-10)
```

### Skill Group

```
Each skill group (4 groups: Languages, Frontend, Backend & Infra, Craft):
  Group name header:
    mono 12/600, uppercase, letter-spacing 1, --ms-fg, margin-bottom: 16px (--space-7)
    border-bottom: 1px var(--ms-border), padding-bottom: 8px (--space-3)

  Skill item rows (flex column, gap: 12px --space-5):
    Each row:
      display: flex
      flex-direction: column
      gap: 4px (--space-1)

      Label row:
        display: flex
        justify-content: space-between
        Skill name: sans 16/400, --ms-fg-soft
        Proficiency number: mono 12/400, --ms-fg-faint  (e.g. "8/10")

      Bar:
        display: flex
        gap: 4px (--space-1)
        height: 8px
        width: 100%

        Each of 10 segments:
          flex: 1
          height: 8px
          border-radius: --radius-xs (2px)
          Filled (index < proficiency): background --ms-orange
          Empty (index >= proficiency): background --ms-border-strong
```

### Skills Groups and Proficiency Data

```
Group: Languages
  TypeScript   9/10
  Python       7/10
  Swift        7/10
  SQL          7/10
  Bash         5/10

Group: Frontend
  React / Next.js   9/10
  SwiftUI           8/10
  CSS / Tailwind    9/10
  Figma             7/10

Group: Backend & Infra
  Node / Express    8/10
  PostgreSQL        7/10
  Docker            6/10
  AWS               6/10
  Sanity CMS        8/10

Group: Craft
  Product thinking  9/10
  API design        8/10
  Performance       8/10
  Accessibility     7/10
```

Note: These are placeholder values. Daniel provides real proficiency data during content
population. Executor should read proficiency from Sanity `profile.professionalSkills`
(already in GROQ projection as `professionalSkills` array) if structured, or hardcode
as static data pending CMS extension. Prefer hardcoded static data for Phase 4 — CMS
extension for skills is deferred.

### Services Section

```
<section id="services">
  padding: 120px 32px (--section-py / --page-px)
  background: --ms-bg-alt
  border-top: 1px var(--ms-border)
  border-bottom: 1px var(--ms-border)

  Inner: max-w 1240px, margin: 0 auto

  <Kicker n="05" label="Services" />

  H2:
    margin-top: 36px
    "What I offer"
    font: sans clamp(36px, 5vw, 88px)/400, tracking -0.035em, line-height 1.02, --ms-fg

  2×2 service card grid:
    margin-top: 48px (--space-15)
    display: grid
    grid-template-columns: repeat(2, 1fr)
    gap: 24px (--space-10)
```

### Service Card

```
Each card (4 total):
  position: relative
  overflow: hidden
  padding: 32px (--space-12)
  background: --ms-surface
  border: 1px var(--ms-border)
  border-radius: --radius-2xl (12px)
  transition: border-color 0.2s (--anim-hover)

  Hover state:
    border-color: var(--ms-orange)  [the orange border highlight per SVC-02]

  Decorative number (aria-hidden):
    position: absolute
    top: -8px
    right: 16px
    font: Fraunces italic 400, clamp(80px, 10vw, 140px), line-height 0.85
    color: var(--ms-border-strong)
    user-select: none
    aria-hidden: "true"
    Numbers: "01." "02." "03." "04."

  Card content (position: relative; z-index: 1):
    Title:
      font: sans 26px/600 (--text-h3), tracking -0.02em, --ms-fg
      max-width: 280px

    Description:
      margin-top: 16px (--space-7)
      font: sans 16/400 (--text-body), line-height 1.6, --ms-fg-soft

    Stack chips row:
      margin-top: 24px (--space-10)
      display: flex, flex-wrap: wrap, gap: 8px (--space-3)
      Each chip:
        mono 12/400, --ms-fg-faint
        padding: 4px 8px
        border: 1px var(--ms-border)
        border-radius: --radius-pill
```

### Service Card Definitions

```
Card 1 — "01."
  Title: "Full-Stack Web"
  Description: "End-to-end product development — from API design to shipped UI.
    React, Next.js, Node, and whatever the project actually needs."
  Stack: Next.js · TypeScript · PostgreSQL · Tailwind

Card 2 — "02."
  Title: "iOS Development"
  Description: "Native Swift and SwiftUI apps. I care about how they feel in your
    hand as much as what they do."
  Stack: Swift · SwiftUI · Xcode · Core Data

Card 3 — "03."
  Title: "Consulting & Rescue"
  Description: "Inherited a mess? I audit, stabilise, and hand over a codebase you
    can confidently build on."
  Stack: Audit · Refactor · Docs · CI

Card 4 — "04."
  Title: "Design Systems"
  Description: "Tokens, components, and the documentation that makes a team fast.
    I've built them from scratch and inherited them mid-project."
  Stack: Figma Tokens · Tailwind · shadcn · Storybook
```

Note: These are placeholder values consistent with Daniel's background and the site's
core value. Daniel may revise during content population — Executor should hardcode these
as static strings in the component. SVC-01 and SVC-02 do not require Sanity integration
for v1.0.

---

## Animation Contracts

| Animation | Keyframe | Used by | Trigger | Reduced-motion |
|-----------|----------|---------|---------|----------------|
| `ms-pulse` | `scale 1→2.6, opacity 0.5→0, 2.2s` (existing) | Current role timeline dot pulse ring | Always running | Disabled by Phase 1 `@media (prefers-reduced-motion)` rule |
| Skill bar fill | No animation — static render | Skill segments | Page load | N/A (no animation) |
| Service card border | CSS `border-color 0.2s` (--anim-hover) | Card on hover | Hover | Degrades gracefully |

Phase 1 declares `--anim-pulse: 2.2s`. Use this value for the timeline current-role pulse.
The existing `ms-pulse` keyframe in `globals.css` is already correct — do not redefine it.

---

## Component Inventory

### New components to create in this phase

| Component | File | Type | Description |
|-----------|------|------|-------------|
| `ExperienceSection` | `components/ExperienceSection.tsx` | `"use client"` | Experience timeline + Education list + Community rows; reads `workExperience` and `education` from GlobalContext |
| `SkillsSection` | `components/SkillsSection.tsx` | server component | Static skills bar groups — no GlobalContext needed; all data is hardcoded for Phase 4 |
| `ServicesSection` | `components/ServicesSection.tsx` | server component | 2×2 service card grid — no GlobalContext needed; all data is hardcoded for Phase 4 |

### Reuse from prior phases

| Component | File | Notes |
|-----------|------|-------|
| `GlobalContext` | `app/context/GlobalContext.tsx` | Read `workExperience` and `education` arrays in `ExperienceSection` |
| `badge` | `components/ui/badge.tsx` | Stack chips on service cards — use outline variant |

### shadcn components — no new installs required

No new shadcn components needed. All UI is built from design tokens and custom components.

---

## Semantic HTML Structure

```html
<main>
  <!-- HeroSection (Phase 2) -->
  <!-- AboutSection (Phase 3) -->
  <!-- WorkSection (Phase 3) -->

  <section id="experience">          <!-- ExperienceSection -->
    <div class="inner">
      <!-- Kicker 03 -->
      <h2>...</h2>
      <div class="exp-grid">         <!-- 1fr 1fr -->
        <div class="timeline">
          <!-- 4 timeline entries -->
        </div>
        <div class="edu-community">
          <!-- education list -->
          <!-- community sub-section -->
        </div>
      </div>
    </div>
  </section>

  <section id="skills">              <!-- SkillsSection -->
    <div class="inner">
      <!-- Kicker 04 -->
      <h2>...</h2>
      <div class="skills-grid">     <!-- repeat(4, 1fr) -->
        <!-- 4 skill groups -->
      </div>
    </div>
  </section>

  <section id="services">           <!-- ServicesSection -->
    <div class="inner">
      <!-- Kicker 05 -->
      <h2>...</h2>
      <div class="services-grid">   <!-- repeat(2, 1fr) -->
        <!-- 4 service cards -->
      </div>
    </div>
  </section>

  <!-- Phase 5+ sections -->
</main>
```

Section `id` attributes will activate the nav links `#experience`, `#skills`, and `#services`
from `SiteHeader.tsx` once the numbered nav links are wired in Phase 2 completion or Phase 6.

---

## Interaction Contract

| Interaction | Trigger | Response |
|-------------|---------|----------|
| Timeline current role dot | Always | `ms-pulse` animation runs on the orange dot ring continuously |
| Service card hover | Mouse enter | `border-color` transitions from `--ms-border` to `--ms-orange` in 0.2s |
| Service card hover — leave | Mouse leave | `border-color` transitions back to `--ms-border` in 0.2s |
| Skill bars | Page load | Segments render filled/empty immediately — no entrance animation |
| Focus ring | Keyboard navigation to any `<a>` or `<button>` | 2px orange ring, `hsl(var(--ring))`, offset 2px |

No accordion, filter, or toggle interactions in this phase. All three sections are read-only
display components.

---

## GlobalContext Data Mapping

`ExperienceSection` is a client component because it reads from `GlobalContext`. Pattern
identical to `AboutSection.tsx` and `WorkSection.tsx`:

```typescript
"use client";
import { useContext } from "react";
import GlobalContext from "@/app/context/GlobalContext";

export default function ExperienceSection() {
  const ctx = useContext(GlobalContext);
  const workExperience = ctx?.workExperience ?? [];
  const education = ctx?.education ?? [];
  // ...
}
```

`SkillsSection` and `ServicesSection` are server components — no `useContext` needed.
All data is hardcoded as static arrays in the component file.

### workExperience fields available (from existing Sanity schema)

Fields already in the GROQ projection: `_id`, `_type`, `title`, `company`, `location`,
`startDate`, `endDate`, `description`, `current`.

- `title` → role title
- `company` → company name
- `startDate` / `endDate` → year range (format: `new Date(startDate).getFullYear()`)
- `current` → boolean — if true, render orange pulse dot + "Current" badge
- `description` → body text paragraph (may be blockContent — check sanityTypes.ts)

### education fields available (from existing Sanity schema)

Fields: `_id`, `_type`, `title`, `school`, `startDate`, `endDate`, `fieldOfStudy`.

- `title` → degree title
- `school` → institution name
- `startDate` / `endDate` → year range
- `fieldOfStudy` → detail line (show if present)

Note: If `description` on `workExperience` is `blockContent` (array of blocks), render
plain text by joining block children. Phase 3 PATTERNS.md shows the `blockContent` type
exists — check `app/models/sanityTypes.ts` and render accordingly. Prefer a simple
`block.children.map(c => c.text).join('')` over installing a block renderer.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Experience + Education kicker | "03 ── EXPERIENCE + EDUCATION" |
| Experience + Education H2 | "Experience + *Education*" |
| Skills kicker | "04 ── SKILLS" |
| Skills H2 | "What I work with" |
| Services kicker | "05 ── SERVICES" |
| Services H2 | "What I offer" |
| Timeline "Current" badge | "Current" |
| Education section label | "EDUCATION" (mono uppercase, not a heading — decorative label) |
| Community section heading | "ALSO / COMMUNITY" |
| Community row 1 | "Open source contributor" · "GitHub / @danmunro" |
| Community row 2 | "Tech speaker" · "GothenburgJS, 2023" |
| Community row 3 | "Mentor" · "Hack Your Future, SE" |
| Service card 1 title | "Full-Stack Web" |
| Service card 2 title | "iOS Development" |
| Service card 3 title | "Consulting & Rescue" |
| Service card 4 title | "Design Systems" |

Note: Community and service card copy is placeholder. Daniel provides real data during
content population. Executor should hardcode these as static strings — no Sanity
integration needed for Phase 4 community rows or service cards.

Empty state — Experience: No `workExperience` data from Sanity → render nothing (section
collapses gracefully). Do not render an empty-state placeholder for Phase 4 timeline.

Empty state — Education: No education data → same; render nothing.

Error state: Not applicable — sections read from GlobalContext (already fetched server-side).

Destructive actions: None in this phase.

---

## Accessibility Contract

| Element | Requirement |
|---------|-------------|
| `<section id="experience">` | Plain section — heading provides context |
| `<section id="skills">` | Plain section — heading provides context |
| `<section id="services">` | Plain section — heading provides context |
| Timeline pulse dot (current role) | `aria-hidden="true"` — decorative animation |
| Timeline "Current" badge | Visible text; readable by screen reader |
| Skill bar segments (filled/empty divs) | Container `role="img"` + `aria-label="TypeScript: 9 out of 10"` per skill item |
| Service card decorative number | `aria-hidden="true"` |
| Service card | Plain `<div>` — no interactive role (no button/link). Cards are display-only per SVC-01/SVC-02 requirements. |
| Service card stack chips | `aria-hidden="true"` — supplementary context |
| Kicker decorative line | `aria-hidden="true"` |
| Focus ring | All `<a>` and `<button>`: `focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2` (orange via `hsl(var(--ring))`) |

---

## Border Radius — Phase 4 Usage

| Element | Token | Value |
|---------|-------|-------|
| Timeline dot (current role) | n/a | `border-radius: 50%` |
| Timeline dot (past roles) | n/a | `border-radius: 50%` |
| Timeline "Current" badge | `--radius-pill` | 999px |
| Skill bar segment | `--radius-xs` | 2px |
| Service card container | `--radius-2xl` | 12px |
| Service card stack chips | `--radius-pill` | 999px |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official (`https://ui.shadcn.com/r/...`) | badge (already installed) | Not required — official registry |
| Third-party | None | Not applicable |

No new shadcn components are installed in Phase 4. The `badge` component already installed
is used for service card stack chips (outline variant). All other UI is built from design
tokens and custom components.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
