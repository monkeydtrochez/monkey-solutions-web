---
phase: 04-experience-skills-services
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sanity/schemaTypes/workExperience.ts
  - sanity/schemaTypes/education.ts
  - app/models/sanityTypes.ts
  - lib/api/sanityDataLoader.ts
  - app/context/GlobalContext.tsx
autonomous: true
requirements: [EXP-01, EXP-02]
must_haves:
  truths:
    - "D-01: D-02: Sanity Studio editors can set `company` and `current` on workExperience documents"
    - "D-03: `location` field is NOT present in workExperience schema"
    - "D-08: Sanity Studio editors can set `fieldOfStudy` on education documents"
    - "D-04: D-09: GROQ projection returns `company`, `current` on workExperience and `fieldOfStudy` on education"
    - "D-07: GlobalContext exposes `education` as an array (Education[] | null), not a single item"
    - "D-05: D-10: TypeScript types match the new GROQ contract — `npm run build` passes"
  artifacts:
    - path: "sanity/schemaTypes/workExperience.ts"
      provides: "company + current fields on workExperience document"
      contains: "name: 'company'"
    - path: "sanity/schemaTypes/education.ts"
      provides: "fieldOfStudy field on education document"
      contains: "name: 'fieldOfStudy'"
    - path: "app/models/sanityTypes.ts"
      provides: "company?, current? on WorkExperience; fieldOfStudy? on Education"
      contains: "company?: string"
    - path: "lib/api/sanityDataLoader.ts"
      provides: "GROQ projection with company, current, fieldOfStudy"
      contains: "fieldOfStudy"
    - path: "app/context/GlobalContext.tsx"
      provides: "education state typed as Education[] | null with filter()"
      contains: "Education[] | null"
  key_links:
    - from: "lib/api/sanityDataLoader.ts"
      to: "app/models/sanityTypes.ts"
      via: "GROQ projection fields match TypeScript interface fields"
      pattern: "company.*current"
    - from: "app/context/GlobalContext.tsx"
      to: "app/models/sanityTypes.ts"
      via: "education state uses Education[] type"
      pattern: "Education\\[\\] \\| null"
---

<objective>
Extend the data layer to support Phase 4 fields and fix the education singleton bug.

Purpose: Phase 4 sections (Experience timeline, Education list) cannot render correctly until the Sanity schema, GROQ projection, TypeScript types, and GlobalContext all carry the new `company`, `current`, and `fieldOfStudy` fields, and until education is treated as an array (mirroring workExperience). Track A in the research — must complete before any component work.

Output: Updated Sanity schemas (workExperience + education), TypeScript interfaces (sanityTypes.ts), GROQ projection (sanityDataLoader.ts), and GlobalContext (education as array). All locked decisions D-01, D-02, D-04, D-05, D-07, D-08, D-09, D-10 implemented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-experience-skills-services/04-CONTEXT.md
@.planning/phases/04-experience-skills-services/04-RESEARCH.md
@.planning/phases/04-experience-skills-services/04-PATTERNS.md
@CLAUDE.md
@sanity/schemaTypes/workExperience.ts
@sanity/schemaTypes/education.ts
@app/models/sanityTypes.ts
@lib/api/sanityDataLoader.ts
@app/context/GlobalContext.tsx

<interfaces>
<!-- Authoritative contracts from current codebase (verified by direct read 2026-05-10). -->
<!-- Executor must respect these field names exactly — do NOT rename. -->

From app/models/sanityTypes.ts (current state):
```typescript
export interface Education extends BaseType {
  _type: "education";
  school: string;
  start: string;    // year string, e.g. "2015"
  end: string;      // year string, e.g. "2019"
  // PHASE 4 ADD: fieldOfStudy?: string
}

export interface WorkExperience extends BaseType {
  _type: "workExperience";
  sortIndex: number;
  duration: Duration;                       // has startYear + endYear (NOT startDate/endDate)
  description: WorkDescriptionBlock[];      // blockContent
  // PHASE 4 ADD: company?: string
  // PHASE 4 ADD: current?: boolean
}

interface Duration {
  _type: "duration";
  startYear: string;
  endYear: string;
}

export type SanityApiResponse = Profile | Education | WorkExperience | Project;
```

From app/context/GlobalContext.tsx (current state — bug to fix per D-07):
```typescript
// CURRENT (singleton via .find() — BUG):
education: Education | null;
const [education, setEducationData] = useState<Education | null>(null);
const educationData = data?.find((item) => item._type === "education");
if (educationData) { setEducationData(educationData); }

// REQUIRED AFTER FIX (mirrors the existing workExperience pattern at lines 45-49):
education: Education[] | null;
const [education, setEducationData] = useState<Education[] | null>(null);
const isEducation = (item: SanityApiResponse): item is Education =>
  item._type === "education";
const educationArray = data?.filter(isEducation) ?? [];
setEducationData(educationArray);
```

From sanity/schemaTypes/workExperience.ts (existing field structure to follow):
```typescript
defineField({ name: 'sortIndex', title: 'Sort Index', type: 'number', validation: ... }),
defineField({ name: 'title', title: 'Title', type: 'string' }),
defineField({ name: 'duration', title: 'Duration', type: 'duration' }),
defineField({ name: 'description', title: 'Work Description', type: 'blockContent' }),
```

From sanity/schemaTypes/education.ts (existing field structure to follow):
```typescript
defineField({ name: 'title', title: 'Title', type: 'string' }),
defineField({ name: 'school', title: 'School', type: 'string' }),
defineField({ name: 'start', title: 'Start', type: 'string', options: { list: lastFiftyYears() } }),
defineField({ name: 'end', title: 'End', type: 'string', options: { list: lastFiftyYears() } }),
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Sanity schemas with company, current, and fieldOfStudy</name>
  <read_first>
    - sanity/schemaTypes/workExperience.ts (current schema — 30 lines, full file)
    - sanity/schemaTypes/education.ts (current schema — 45 lines, full file)
    - .planning/phases/04-experience-skills-services/04-CONTEXT.md (D-01, D-02, D-03, D-08)
  </read_first>
  <files>sanity/schemaTypes/workExperience.ts, sanity/schemaTypes/education.ts</files>
  <action>
Edit `sanity/schemaTypes/workExperience.ts`. After the existing `description` field (line 28), append two new `defineField` entries inside the `fields` array (do NOT add a `location` field per D-03):

```typescript
defineField({
  name: 'company',
  title: 'Company',
  type: 'string',
  description: 'The employer or client organisation name displayed in the timeline.',
}),
defineField({
  name: 'current',
  title: 'Current role',
  type: 'boolean',
  description: 'Enable for the active/current job. Controls the orange pulse dot in the timeline.',
  initialValue: false,
}),
```

Edit `sanity/schemaTypes/education.ts`. After the existing `end` field (line 43, the closing `})` of the end field), append one new `defineField` entry inside the `fields` array:

```typescript
defineField({
  name: 'fieldOfStudy',
  title: 'Field of Study',
  type: 'string',
  description: 'Degree or major detail line, e.g. "Computer Science". Renders below the school name if present.',
}),
```

Do not remove or reorder any existing fields. Keep `defineField`/`defineType` imports unchanged.
  </action>
  <verify>
    <automated>grep -q "name: 'company'" sanity/schemaTypes/workExperience.ts && grep -q "name: 'current'" sanity/schemaTypes/workExperience.ts && grep -q "initialValue: false" sanity/schemaTypes/workExperience.ts && grep -q "name: 'fieldOfStudy'" sanity/schemaTypes/education.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "name: 'company'" sanity/schemaTypes/workExperience.ts` returns 1
    - `grep -c "name: 'current'" sanity/schemaTypes/workExperience.ts` returns 1
    - `grep -c "initialValue: false" sanity/schemaTypes/workExperience.ts` returns 1
    - `grep -c "type: 'boolean'" sanity/schemaTypes/workExperience.ts` returns 1
    - `grep -c "name: 'fieldOfStudy'" sanity/schemaTypes/education.ts` returns 1
    - `grep -c "name: 'location'" sanity/schemaTypes/workExperience.ts` returns 0 (D-03: location NOT added)
    - All existing fields still present: `grep -c "name: 'sortIndex'" sanity/schemaTypes/workExperience.ts` returns 1; `grep -c "name: 'school'" sanity/schemaTypes/education.ts` returns 1
  </acceptance_criteria>
  <done>workExperience schema has `company: string` and `current: boolean` (initialValue false); education schema has `fieldOfStudy: string`; no other field changes.</done>
</task>

<task type="auto">
  <name>Task 2: Update TypeScript types and GROQ projection</name>
  <read_first>
    - app/models/sanityTypes.ts (current types — 100 lines, full file)
    - lib/api/sanityDataLoader.ts (current GROQ — 77 lines, full file)
    - .planning/phases/04-experience-skills-services/04-CONTEXT.md (D-04, D-05, D-09, D-10)
    - .planning/phases/04-experience-skills-services/04-PATTERNS.md (lines 477-501 — GROQ projection diff)
  </read_first>
  <files>app/models/sanityTypes.ts, lib/api/sanityDataLoader.ts</files>
  <action>
Edit `app/models/sanityTypes.ts`:

1. In the `Education` interface (lines 27-32), add `fieldOfStudy?: string;` as an optional property after `end: string;`. Final shape:
```typescript
export interface Education extends BaseType {
  _type: "education";
  school: string;
  start: string;
  end: string;
  fieldOfStudy?: string;
}
```
Do NOT rename `start`/`end` to `startDate`/`endDate` (D-10 locks the field names).

2. In the `WorkExperience` interface (lines 35-40), add `company?: string;` and `current?: boolean;` as optional properties after `description`. Final shape:
```typescript
export interface WorkExperience extends BaseType {
  _type: "workExperience";
  sortIndex: number;
  duration: Duration;
  description: WorkDescriptionBlock[];
  company?: string;
  current?: boolean;
}
```

Do not change the `Duration` interface (`startYear`/`endYear` field names are correct — D-10).

Edit `lib/api/sanityDataLoader.ts`:

3. Update the GROQ projection inside the `query` template literal:
   - Education block (lines 25-29) — add `fieldOfStudy` after `end`. After edit:
     ```groq
     _type == 'education' => {
       school,
       start,
       end,
       fieldOfStudy
     },
     ```
   - workExperience block (lines 31-35) — add `company` and `current` after `description`. After edit:
     ```groq
     _type == 'workExperience' => {
       sortIndex,
       duration,
       description,
       company,
       current
     },
     ```
Do not touch the profile or project projections. Do not change the query's outer filter or other parts of the file.
  </action>
  <verify>
    <automated>grep -q "fieldOfStudy?: string" app/models/sanityTypes.ts && grep -q "company?: string" app/models/sanityTypes.ts && grep -q "current?: boolean" app/models/sanityTypes.ts && grep -q "fieldOfStudy" lib/api/sanityDataLoader.ts && grep -q "company" lib/api/sanityDataLoader.ts && grep -q "current" lib/api/sanityDataLoader.ts && npx tsc --noEmit 2>&1 | (! grep -E "error TS")</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fieldOfStudy?: string" app/models/sanityTypes.ts` returns 1
    - `grep -c "company?: string" app/models/sanityTypes.ts` returns 1
    - `grep -c "current?: boolean" app/models/sanityTypes.ts` returns 1
    - `grep -c "startYear: string" app/models/sanityTypes.ts` returns 1 (Duration unchanged)
    - `grep -c "fieldOfStudy" lib/api/sanityDataLoader.ts` returns 1
    - `grep -c "company" lib/api/sanityDataLoader.ts` returns at least 1
    - `grep -c "current" lib/api/sanityDataLoader.ts` returns at least 1
    - `npx tsc --noEmit` exits 0 (no TypeScript errors)
    - `grep -c "startDate" app/models/sanityTypes.ts` returns 0 (D-10: NOT renamed)
  </acceptance_criteria>
  <done>WorkExperience and Education interfaces have new optional fields; GROQ projection requests new fields; TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 3: Fix GlobalContext education singleton — make it an array</name>
  <read_first>
    - app/context/GlobalContext.tsx (current context — 76 lines, full file)
    - .planning/phases/04-experience-skills-services/04-CONTEXT.md (D-07)
    - .planning/phases/04-experience-skills-services/04-PATTERNS.md (lines 504-535 — exact diff for GlobalContext)
  </read_first>
  <files>app/context/GlobalContext.tsx</files>
  <action>
Edit `app/context/GlobalContext.tsx` to make `education` an array, mirroring the existing `workExperience` pattern (already correct at lines 45-49):

1. In the `ContextType` interface (line 13), change `education: Education | null;` to `education: Education[] | null;`.

2. In the `GlobalContextProvider` body (line 27), change `const [education, setEducationData] = useState<Education | null>(null);` to `const [education, setEducationData] = useState<Education[] | null>(null);`.

3. In `setSiteContentToContext` (lines 40-43), replace this block:
```typescript
const educationData = data?.find((item) => item._type === "education");
if (educationData) {
  setEducationData(educationData);
}
```
with this block (uses `.filter()` via a type predicate, mirroring the workExperience pattern below it on lines 45-49):
```typescript
const isEducation = (item: SanityApiResponse): item is Education =>
  item._type === "education";
const educationArray = data?.filter(isEducation) ?? [];
setEducationData(educationArray);
```

Do not touch the `profile`, `workExperience`, or `projects` state. Keep all imports unchanged. The `Education` type import is already present (line 6).
  </action>
  <verify>
    <automated>grep -q "education: Education\[\] | null" app/context/GlobalContext.tsx && grep -q "useState<Education\[\] | null>" app/context/GlobalContext.tsx && grep -q "isEducation" app/context/GlobalContext.tsx && ! grep -q 'data?.find((item) => item._type === "education")' app/context/GlobalContext.tsx && npx tsc --noEmit 2>&1 | (! grep -E "error TS")</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "education: Education\[\] | null" app/context/GlobalContext.tsx` returns 1
    - `grep -c "useState<Education\[\] | null>" app/context/GlobalContext.tsx` returns 1
    - `grep -c "isEducation" app/context/GlobalContext.tsx` returns at least 1 (type predicate added)
    - `grep -c "data?.filter(isEducation)" app/context/GlobalContext.tsx` returns 1
    - `grep -c 'data?.find((item) => item._type === "education")' app/context/GlobalContext.tsx` returns 0 (old code removed)
    - `grep -c "Education | null" app/context/GlobalContext.tsx | grep -v "Education\[\]"` produces no lines (no singleton remnants)
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>GlobalContext's education state is `Education[] | null`, populated via `.filter()` not `.find()`. TypeScript compiles. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sanity CMS → Next.js server | Content authored in Sanity Studio is fetched server-side via GROQ. No user-supplied input crosses this boundary in Phase 4. |
| Server → Client (GlobalContext) | Server data is hydrated into a React context. Read-only; no client-originated mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | sanity/schemaTypes/workExperience.ts new fields | accept | Schema fields are typed (`string`, `boolean`). Studio enforces type at edit time. No user input from public web. |
| T-04-02 | Information Disclosure | lib/api/sanityDataLoader.ts GROQ projection | accept | All three new fields (`company`, `current`, `fieldOfStudy`) are intentionally public CV content. No PII or secrets. |
| T-04-03 | Denial of Service | GROQ projection adding 3 fields | accept | Additive projection on existing query. Sanity CDN-off setting already in place; no perf change. |

**No attack surface added.** Phase 4 introduces no auth, no session, no input processing, no cryptography. ASVS L1 categories do not apply to additive display-only data fields.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npm run build` exits 0
- `grep -nE "name: 'company'|name: 'current'|name: 'fieldOfStudy'" sanity/schemaTypes/*.ts` shows the three new field definitions
- `grep -nE "company\\?:|current\\?:|fieldOfStudy\\?:" app/models/sanityTypes.ts` shows the three new TypeScript optional properties
- `grep -nE "company|current|fieldOfStudy" lib/api/sanityDataLoader.ts` shows fields in the GROQ projection
- `grep -nE "Education\\[\\] \\| null|isEducation" app/context/GlobalContext.tsx` shows education-as-array pattern
</verification>

<success_criteria>
- All three schema field additions complete (D-01, D-02, D-08)
- All three TypeScript field additions complete (D-05, D-10)
- GROQ projection requests new fields (D-04, D-09)
- GlobalContext fix applied (D-07): education is `Education[] | null` populated via `.filter()`
- TypeScript compiles with no new errors
- `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/04-experience-skills-services/04-01-SUMMARY.md` documenting:
- The five files modified
- The exact set of D-IDs implemented (D-01, D-02, D-04, D-05, D-07, D-08, D-09, D-10)
- Confirmation that D-03 was honored (no `location` field added)
- Confirmation that field names `start`/`end` on Education were preserved per D-10
- Note: Sanity Studio redeploy (`cd sanity && npm run deploy`) needed to make new fields editable in hosted Studio — this is a manual step Daniel runs (not blocking for downstream component plans, which only consume the type changes).
</output>
</content>
</invoke>